[package]
name = "day-22"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools.workspace = true

[dev-dependencies]
rstest = {version = "0.23.0", default-features = false, features = ["crate-name"]}
